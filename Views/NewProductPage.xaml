<ContentPage
    x:Class="SnapLabel.Views.NewProductPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:constant="clr-namespace:SnapLabel.Constants"
    xmlns:effect="clr-namespace:Syncfusion.Maui.Core;assembly=Syncfusion.Maui.Core"
    xmlns:helpers="clr-namespace:SnapLabel.Helpers"
    xmlns:input="clr-namespace:Syncfusion.Maui.Inputs;assembly=Syncfusion.Maui.Inputs"
    xmlns:inputLayout="clr-namespace:Syncfusion.Maui.Core;assembly=Syncfusion.Maui.Core"
    xmlns:vm="clr-namespace:SnapLabel.ViewModels"
    Title="New Product"
    x:DataType="vm:NewProductPageViewModel">

    <ContentPage.ToolbarItems>
        <ToolbarItem Command="{Binding SaveProductCommand}">
            <ToolbarItem.IconImageSource>
                <FontImageSource
                    FontFamily="Mat"
                    Glyph="{Static constant:FontsConstants.Save}"
                    Color="{AppThemeBinding Dark={StaticResource White},
                                            Light={StaticResource Black}}" />
            </ToolbarItem.IconImageSource>
        </ToolbarItem>
    </ContentPage.ToolbarItems>

    <ScrollView>
        <VerticalStackLayout Padding="20">

            <!--  Name  -->
            <inputLayout:SfTextInputLayout Padding="8,0,0,0" Hint="Name">
                <Entry
                    Keyboard="Text"
                    Text="{Binding ProductVM.Name}"
                    VerticalTextAlignment="Center" />
            </inputLayout:SfTextInputLayout>

            <!--  Location  -->
            <inputLayout:SfTextInputLayout Padding="8,0,0,0" Hint="Loation">
                <Entry
                    Keyboard="Text"
                    Text="{Binding ProductVM.Location}"
                    VerticalTextAlignment="Center" />
            </inputLayout:SfTextInputLayout>


            <!--  Price  -->
            <inputLayout:SfTextInputLayout Padding="8,0,0,0" Hint="Price">
                <Entry
                    Keyboard="Numeric"
                    Text="{Binding ProductVM.Price}"
                    VerticalTextAlignment="Center" />
            </inputLayout:SfTextInputLayout>

            <!--  Capture Image Button  -->
            <Button
                Grid.Row="3"
                BackgroundColor="#2196F3"
                Command="{Binding CaptureImageCommand}"
                CornerRadius="8"
                FontSize="18"
                HeightRequest="50"
                Text="Capture Image"
                TextColor="White" />

            <!--  Image Preview  -->
            <Border
                Margin="0,20,0,0"
                BackgroundColor="#eee"
                HeightRequest="300"
                Stroke="#ccc"
                WidthRequest="300">
                <Image
                    Margin="10"
                    Aspect="AspectFill"
                    Source="{Binding ProductVM.ImagePreview}" />
                <Border.StrokeShape>
                    <RoundRectangle CornerRadius="8" />
                </Border.StrokeShape>
                <Border.Shadow>
                    <Shadow
                        Brush="{AppThemeBinding Dark={StaticResource White},
                                                Light={StaticResource Black}}"
                        Opacity="0.4"
                        Radius="3"
                        Offset="5,5" />
                </Border.Shadow>
            </Border>


        </VerticalStackLayout>
    </ScrollView>


</ContentPage>
